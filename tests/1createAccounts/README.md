# Create acoounts helper

### Endpoint:
 - /createaccount

### JSON:

```json
{
  "input": {
    "username": "string@gmail.com", 
    "password": "string", 
    "confirmation": "string", 
    "phone": "string", 
    "address": "string", 
    "role": "USER|BOFFICER|ADMIN"
  }
}
```

Gmail, just because it happened, it can be anything as long as it has an @ symbol, as if it were an email address.